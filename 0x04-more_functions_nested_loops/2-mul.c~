#include "main.h"

/**
 * mul - checks for uppercase letters
 * @a: character to return
 * @b: character to return
 * Return: 1 if upper, 0 otherwise
 */

int mul(int a, int b)
{
	int a = 2, int  = 4;

	return (a * b);
}
